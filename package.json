{
  "name": "finops-llm",
  "private": true,
  "scripts": {
    "prebuild": "node scripts/generate-llms-full.js && node scripts/content-gap-analyzer.js",
    "build": "eleventy",
    "serve": "eleventy --serve",
    "test": "node scripts/worker-crawlers.test.mjs && node scripts/worker-translated.test.mjs"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6"
  }
}
